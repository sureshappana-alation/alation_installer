name: deploy
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set current date as env variable
        run: echo "NOW::$(date +'%Y%m%d')" >> $GITHUB_ENV
      - name: Echo current date
        run: echo alation-$GITHUB_REF-$NOW.$GITHUB_RUN_NUMBER
